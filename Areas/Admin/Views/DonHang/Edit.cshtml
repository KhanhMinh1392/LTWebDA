@model Web.Models.ChiTietDonDatHang
@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<style>
    .underline:hover{
        text-decoration:underline;
    }
</style>

     
<main>
    &lsaquo; <a href="@Url.Action("Donhang","Admin")" class="dash-title underline"> Quay về quản lý đơn hàng</a>
    <h2 class="dash-title">Chỉnh Sửa Đơn Hàng</h2>
    <section class="recent">
        <div class="activity-grid">
            <div class="activity-card">
                <h3>Thông Tin Khách Hàng</h3>
                
                <div class="detail">
                    @using (Html.BeginForm("Edit", "Add", FormMethod.Post, new { @enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(m=>m.DONDATHANG_ID)
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <table>
                        <thread>
                            <tr>
                                <th>Mã ĐH</th>
                                <th>Tên sản phẩm</th>
                                <th>Số lượng</th>
                                <th>Đơn giá</th>
                                <th>Chiết khấu</th>
                                <th>Thành tiền</th>
                            </tr>
                        </thread>
                        <tbody>
                                <tr>
                                    @*<td class="content"><a href="#">@item.DONDATHANG_ID</a></td>
                                    <td>@item.TENSP</td>
                                    <td class="content">@item.SOLUONG</td>
                                    <td class="content">@item.GIA</td>
                                    <td class="content">0%</td>
                                    <td class="content">@item.THANHTIEN</td>*@
                                </tr>
                        </tbody>
                    </table>
                        <div class="btn">
                            <input type="submit" value="Sửa">
                        </div>}
                </div>
            </div>
            <div class="summary">
                <div class="summary-card">
                    <div class="summary-single">
                        <h3>Thông tin đơn hàng</h3>
                    </div>
                    <div class="summary-single">
                        <h4>Bảng Giá: </h4>
                    </div>
                    <div class="summary-single">

                    </div>
                    <div class="summary-single">

                    </div>
                </div>
            </div>
        </div>
    </section>
</main>



